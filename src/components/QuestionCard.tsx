const React = require('react')

type Props = {
    question: string
    answer: string[]
    callback: any
    userAnswer: string
    questionNumber: number
    totalQuestions: number
}

const QuestionCard = () => {
    return (
        <div>
            Question Card
        </div>
    )
}

export default QuestionCard